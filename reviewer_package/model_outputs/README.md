# What every model was given, and what every model returns

Generated by `src/brainsafe/analysis/build_reviewer_matrix.py` from the artefacts on disk. Every
figure is read from the endpoint tables, the per-model metadata written at training time, or
`binder_modes.json`. Nothing is restated from a document, and where an artefact does not exist the
cell is empty and named in `BLANKS.csv` rather than filled with a plausible value.

**Regenerated after the audit fixes.** These are the models retrained with duplicates collapsed
before splitting, actives withheld by scaffold, thresholds set from withheld inactives, and
background pools kept disjoint. Figures here will not match documents written before that pass.

---

## How many outputs there are

**69 outputs are scored per compound. 71 models exist; two are withdrawn.**

| Family | Models | Scored | What one output is |
|---|---|---|---|
| Target classifier | 8 | 8 | probability the compound is active at that target |
| Potency regression | 5 | 5 | pChEMBL potency (4 receptors) or DPPH pIC50 (antioxidant) |
| Binder classifier | 52 | **47** | probability of engagement, called against a per-target threshold |
| ADME / exposure | 9 | 9 | exposure or developability property |
| **Total** | **74** | **69** | |

Five are withdrawn and never scored: **Nav1_1**, **Cav3_2**, **NRF2**, **NFKB1** and **NR3C1**.
The first two have no probability band any
threshold can cut. Every trivial metabolite scores 0.80–0.82 against Nav1_1's 0.796 threshold, and
atenolol scores 0.084 against Cav3_2's 0.065. The last three were added to test natural-product
coverage and failed against their own held-out measured inactives, at AUROC 0.539, 0.392 and 0.479.
All five remain in `OUTPUT_CATALOGUE.csv` with `deployed = False` so the decision is visible rather
than the models simply being absent.

**On the figure 66.** No grouping of these artefacts produces 66. The counts that do occur are 69
(scored outputs), 71 (models on disk), 52 (distinct target names in the knowledge graph, of which one
is the neuroprotection axis rather than a protein), and 81 (69 outputs plus the 12 input
descriptors, which are inputs and not predictions). 66 is stated here as not reproducible from the
repository so that it is corrected rather than carried forward.

Four endpoints carry **two** models each and so appear twice: **D2, A2A, 5-HT2A and SERT** have both
a potency regression and a binder classifier. That is the main reason a count taken from endpoint
names disagrees with one taken from models.

---

## The files

### `TRAINING_MATRIX.csv` — what was measured, for which compound

**174,612 compounds x 131 measured columns.** One row per compound, keyed by the InChIKey of the
desalted parent, which is how the endpoint tables are deduplicated.

| Column | Meaning |
|---|---|
| `inchikey` | InChIKey of the desalted parent structure |
| `parent_smiles` | canonical SMILES of that parent |
| `n_endpoints_measured` | how many endpoint columns are populated for this compound |
| `<endpoint>__label` | 1 active, 0 inactive, as used to train the classifier |
| `<endpoint>__pchembl` | measured potency, -log10 molar |
| `<endpoint>__value` | continuous target for a regression endpoint |
| `adme_<endpoint>__value` | measured ADME property |

**The matrix is deliberately sparse: about 1.9 per cent of cells are populated, and the median
compound carries measurements on 2 endpoints.** That is what the underlying evidence looks like. No
compound has been measured against most targets, and the empty cells are the honest representation
of that. See `BLANKS.csv`: **an empty cell is absent evidence, never a measured zero, and that
compound did not train that model.**

### `TRAINING_SOURCES.csv` — where each column came from

Per endpoint: source file, rows, what was measured, active/inactive counts, and
`rows_sharing_a_parent_inchikey`. That last column records **409 rows across 31 endpoints** where two
rows in one table collapse to a single parent structure, because the expansion fetchers deduplicated
on the raw SMILES string before that was corrected. The first is kept and the count published rather
than the collapse happening silently.

### `OUTPUT_CATALOGUE.csv` — every output, and how it was made

One row per output. `role_in_tool`, the model class and file, training composition, features,
hyper-parameters, the cross-validation scheme and its result, the decision threshold and the basis
that threshold rests on. For binders it also carries the held-out AUROC, sensitivity and its basis,
the held-out background false-positive rate, and whether the endpoint passes the reliability gate.

### `RECIPE_<family>.csv` — the four families separately

Because **the families are trained differently**, and mixing them in one mean is how a panel figure
stops meaning anything:

| File | Family |
|---|---|
| `RECIPE_target_classifier.csv` | the 8 property and target classifiers |
| `RECIPE_potency_regression.csv` | the 4 receptor regressions and the antioxidant model |
| `RECIPE_binder_classifier.csv` | the 52 binder classifiers |
| `RECIPE_ADME___exposure.csv` | the 9 ADME endpoints |

### `BLANKS.csv` — what an empty cell means

Seven distinct kinds of blank, each with its reason.

---

## How each family was trained and tested

### Target classifiers (8) and potency regressions (5) — `train_rf.py`

- **Input:** 1024-bit ECFP-4 (Morgan radius 2, chirality **off**) over the desalted parent, plus 12
  RDKit descriptors. 1,036 features.
- **Labels:** active at pChEMBL >= 6, inactive below 5, the 5–6 band discarded.
- **Deduplication:** rows with identical feature vectors are collapsed **before the split and before
  the deployed refit**. Groups whose members disagree on the label are dropped, not voted on.
  Per-endpoint counts are in the catalogue.
- **Testing:** 10-fold twice, `StratifiedKFold` random and `GroupKFold` on the Bemis-Murcko scaffold
  computed on the same desalted parent the featuriser uses.
- **Deployment:** refit on all remaining data; `RandomForestClassifier(n_estimators=300,
  min_samples_leaf=2, class_weight="balanced", random_state=42)`.
- **Calibration:** isotonic, cross-validated. The eight classifiers have a `_calibrated` companion.

### Binder classifiers (52) — `train_binders_hybrid.py`, `train_measured_label_holdout.py`

- **Positives:** measured binders at pChEMBL >= 7. **A fifth of the active scaffold groups is
  withheld and never trained on**, and sensitivity is reported on those.
- **Negatives:** measured inactives split in half, one half trained on; plus property-matched decoys
  drawn only from the **decoy pool**, with the target's own measured inactives excluded.
- **Threshold:** a quantile of the **withheld** inactives, read from
  `models_rf/holdout/<target>_binder_holdout.json`, combined with a quantile of the **threshold
  pool**.
- **Background false-positive rate:** measured on the **evaluation pool**, disjoint from both. This
  matters: measured on the sample the threshold came from, the rate could not exceed its own target
  and therefore measured nothing.
- **Model:** RandomForest (300 trees, `min_samples_leaf=4`) with sigmoid calibration.

### ADME (9) — `train_adme.py`

Same features and the same CV scheme, importing its configuration from `train_rf.py` rather than
copying it. No calibration and no thresholds: these are read as values, not calls.

---

## Reading the numbers honestly

- **Sensitivity is now measured on withheld actives.** The panel mean fell from 0.8942 to **0.7513**
  when it stopped being measured on the compounds the models were fitted to. The earlier figure was
  recall of the training set.
- **The background false-positive rate can now exceed its target** (maximum 0.0590 against a 0.05
  bound). Before it could not, by construction.
- **Six endpoints fail the reliability gate:** COX2, GBA1, GluA2, Nav1_6, TAAR1 and Nav1_1. That is
  the honest count, against two before.
- **Three binder thresholds are hand-set** and should not be inherited: a3b4nAChR 0.450, SIRT1 0.650,
  RIPK1 0.500. Each was chosen against the **previous** models to stop a specific compound firing, so
  they are both improperly derived and now stale.
- **`cv_scaffold` is the number to read**, not `cv_random`. The random split is the conventional
  estimate; the scaffold split is the one that asks about new chemistry.
